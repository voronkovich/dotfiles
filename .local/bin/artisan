#!/usr/bin/env zsh

# https://github.com/voronkovich/symfony.plugin.zsh?tab=readme-ov-file#configuration

export SF_CONSOLE='artisan'
export SF_SYMFONY_CLI='off'

if [[ -x "${PWD}/vendor/bin/sail" ]]; then
    export SF_RUNNER='docker-compose exec -- laravel.test'
fi

exec sf "$@"
