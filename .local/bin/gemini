#!/usr/bin/bash

readonly pkg='@google/gemini-cli'

export "$(grep GEMINI_API_KEY ${HOME}/.secrets/envs)"

if [ "${1}" = 'mcp' ]; then
    exec npx -y "${pkg}" "$@"
fi

HTTPS_PROXY=http://127.0.0.1:18080 exec npx -y "${pkg}" "$@"
