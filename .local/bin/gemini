#!/usr/bin/env sh

export "$(grep GEMINI_API_KEY ${HOME}/.secrets/envs)"

if [ "${1}" = 'mcp' ]; then
    exec bunx --silent --bun @google/gemini-cli "$@"
fi

HTTPS_PROXY=http://127.0.0.1:18080 exec bunx --silent --bun @google/gemini-cli "$@"
