#!/usr/bin/env sh

export "$(grep GEMINI_API_KEY ${HOME}/.secrets/envs)"

if [ "${1}" = 'mcp' ]; then
    exec bunx @google/gemini-cli "$@"
fi

exec bunx @google/gemini-cli --proxy=http://127.0.0.1:18080 "$@"
