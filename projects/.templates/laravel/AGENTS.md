## Symfony CLI

Use Symfony CLI instead of direct PHP commands for better environment management:

- Artisan: `symfony php artisan route:list`.
- Composer: `symfony composer install`.
- Tests: `APP_ENV=testing symfony php artisan test`.
